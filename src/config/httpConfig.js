/**
 * 此配置为http请求工具配置，
 * baseUrl    为API地址
 * port       为API端口
 * prefix     为API前缀
 * 注： 没有对应属性则给一个空字符串即可。不可为null
 */

export default httpConfig = {
  baseUrl: 'https://api.shifeng1993.com',
  port: '',
  prefix: ''
}